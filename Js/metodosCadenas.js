let cadena = "Cadena de prueba"


.concat("otra cadena") // concatena caddenas
.startsWith("otra cadena") // verifica si una cadena empieza con otra
.endsWith("otra cadena") // verifiva si termina con eso
.includes("otra cadena") // muestra si está
.indexOf("otra cadena") // muestra la posición de la primera letra
.lastIndexOf("otra cadena") // muestra la posición de la primera letra de la ultima palabra
.split("separador") // separa string y transforma en array
.substring(0, 2) // corta string de la posición 0 a la 1, ultimo no incluido
.toLowerCase() // Lo pone todo en minuscula
.toUpperCase() // Lo pone todo en mayuscula
.toString() // convierte a string
.trim() // Elimina los espacios en blanco al final y al inicio 
.trimEnd() // Elimina los espacios en blanco al final
.trimStart() // Elimina los espacios en blanco al inicio
